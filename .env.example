# HyprLab Usage Dashboard Configuration (React Version)
# Copy this file to '.env' and update with your actual values

# API Configuration
REACT_APP_API_BASE_URL=https://api.hyprlab.io/v1

# Note: API key is stored in browser localStorage for security
# You'll enter it through the login form when you first access the app